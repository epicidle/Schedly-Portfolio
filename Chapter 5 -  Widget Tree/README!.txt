Scaffold
└── Stack
    ├── AnimatedPositioned
    │   └── Center
    │       └── Text ('Schedly')
    └── AnimatedOpacity
        └── Padding
            └── Column
                ├── _buildFeatureButton ('Schedule')
                ├── SizedBox
                ├── _buildFeatureButton ('Learning Packets')
                ├── SizedBox
                ├── _buildFeatureButton ('Tasks')
                ├── SizedBox
                └── _buildFeatureButton ('Achievements')

So this is how the widget tree looks like for the provided screenshot,
widgets are inside 'AnimatedPositioned' and 'AnimatedOpacity' for the Animation.